<form method="post" action="{{ path('app_wishlist_item_delete', {'id': wishlist_item.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ wishlist_item.id) }}">
    <button class="btn">Delete</button>
</form>
